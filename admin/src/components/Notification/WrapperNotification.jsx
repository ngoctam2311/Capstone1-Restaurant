import "./notification.css"

const WrapperNotification = ({children}) => {
  return (
    <div className="wrapper-notification">
      {children}
    </div>
  )
}

export default WrapperNotification
